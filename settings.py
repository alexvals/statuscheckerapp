#interval to refresh status codes in seconds
refresh_interval = 60.0
#file containing groups of urls to check in json format
filename = "urls.json"
#message to display if sites are not connectable
site_down = "UNREACHABLE"
